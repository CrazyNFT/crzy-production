import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  // CrzyNFT Theme
  palette: {
    primary: {
      light: "#eff4f7",
      main: "#1c817c",
      dark: "#006260",
    },
  },
});

export default theme;
